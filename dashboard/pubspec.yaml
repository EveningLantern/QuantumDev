name: dashboard
description: "A new Flutter project."
publish_to: 'none'
version: 0.1.0

environment:
  sdk: ^3.6.0

dependencies:
  flutter:
    sdk: flutter
  csv: ^5.1.1
  path_provider: ^2.1.3
  fl_chart: ^0.68.0 
  http: ^1.2.1
  intl: ^0.19.0  # Or the latest version - Add this line
  dotted_border: ^2.1.0
  shared_preferences: ^2.5.3
  flutter_colorpicker: ^1.1.0
  file_picker: ^10.1.9

dev_dependencies:
  flutter_test:
    sdk: flutter
  flutter_lints: ^5.0.0

flutter:
  uses-material-design: true
  assets:
    - assets/ # This line ensures all files in the 'assets' folder are bundled
